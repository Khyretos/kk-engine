# The docs site's build tools (docs/DOCS_SITE.md). MkDocs is held below 2.0:
# 2.0 drops the plugin and theme system Material is built on (see the notice
# Material prints, which NO_MKDOCS_2_WARNING silences once this pin is in).
mkdocs>=1.6,<2
mkdocs-material>=9.7,<10
pymdown-extensions>=10
